<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.product.mapper.ProductSkuMapper">

    <resultMap id="SkuVOMap" type="com.mall.api.product.dto.response.SkuVO">
        <id column="id" property="id"/>
        <result column="spu_id" property="spuId"/>
        <result column="sku_name" property="skuName"/>
        <result column="sku_code" property="skuCode"/>
        <result column="price" property="price"/>
        <result column="original_price" property="originalPrice"/>
        <result column="cost_price" property="costPrice"/>
        <result column="stock" property="stock"/>
        <result column="lock_stock" property="lockStock"/>
        <result column="warn_stock" property="warnStock"/>
        <result column="image" property="image"/>
        <result column="specs" property="specs" typeHandler="com.mall.product.handler.JsonMapTypeHandler"/>
        <result column="weight" property="weight"/>
        <result column="volume" property="volume"/>
        <result column="sale_count" property="saleCount"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectSkusBySpuId" resultMap="SkuVOMap">
        SELECT * FROM product_sku
        WHERE spu_id = #{spuId} AND deleted = 0
        ORDER BY sort ASC, id ASC
    </select>

    <select id="selectSkusByIds" resultMap="SkuVOMap">
        SELECT * FROM product_sku
        WHERE deleted = 0 AND id IN
        <foreach collection="skuIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="checkStock" resultType="java.lang.Integer">
        SELECT stock - lock_stock AS available_stock
        FROM product_sku
        WHERE id = #{skuId} AND deleted = 0
    </select>

</mapper>
